use hex2::Result;

/*impl State {AAA
    pub fn new() -> Self {
        State::Idle
    }

    pub fn process(&mut self, event: Event) -> Result<()> {
        match event {
            Event::NewCard(id) => {
            }
        }
    }
}
*/
