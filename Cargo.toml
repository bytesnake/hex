[workspace]

members = [
    "database",
    "music-container",
    "server",
    "server/protocol",
    "zyklop",
    "cli",
    "nightly-worker",
    "sync"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s" 
debug = false
lto = true
