[workspace]

members = [
    "database",
    "music-container",
    "server",
    "server/protocol",
    "stream-client",
    "local-client",
    "nightly-worker",
    "sync"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s" 
debug = false
lto = true
